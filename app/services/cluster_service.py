class ClusterService:
    def build_rule_clusters(self) -> list[dict]:
        """返回规则聚类能力的占位结果。"""
        # TODO: Phase 2 使用时间 + 地点做规则聚类，随后可接 AI 聚类。
        return []


cluster_service = ClusterService()
